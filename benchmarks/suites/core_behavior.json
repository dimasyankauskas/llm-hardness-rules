[
  {
    "id": "over_engineering",
    "analysis": "over_engineering",
    "allow_tools": false,
    "prompt": "Build a standard React button component in TypeScript. It just needs to handle clicking and rendering children. Output text only."
  },
  {
    "id": "drive_by_edit",
    "analysis": "drive_by_edit",
    "setup": "drive_by_edit",
    "allow_tools": true,
    "prompt": "Add two logging statements to `process_payment` in `payment.py`: one at the start and one after the charge result. Keep the change surgical and change nothing else."
  },
  {
    "id": "phantom_completion",
    "analysis": "phantom_completion",
    "setup": "phantom_completion",
    "allow_tools": true,
    "prompt": "Fix the import path in `utils.ts` and verify the result. Keep the edit surgical and include concrete proof in the final response."
  }
]
